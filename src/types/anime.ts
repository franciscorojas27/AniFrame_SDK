import { Genero } from "../enum/filter.js";

export type anime = {
  name: string;
  cap: string;
  urlReady: string;
  urlImg: string | null;
};

export type animeCatalog = {
  name: string;
  urlReady: string;
  urlImg: string | null;
};

